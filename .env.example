# Optional: GitHub token used SERVER-SIDE ONLY to raise API rate limits.
# Never shipped to the client bundle. Leave empty for unauthenticated access (60 req/h).
GITHUB_TOKEN=

# Public origin used when composing README embed snippets.
# Falls back to the incoming request origin when unset (fine for local dev).
NEXT_PUBLIC_APP_URL=
